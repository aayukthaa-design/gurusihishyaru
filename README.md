
  # Dual-Theme Design System

  This is a code bundle for Dual-Theme Design System. The original project is available at https://www.figma.com/design/w082DBsplTjL1LbLNWcfQP/Dual-Theme-Design-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  